version: '3.8'

services:
  # db:
  #   image: postgres:16-alpine
  #   container_name: career-assistant-db
  #   restart: always

  #   environment:
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: password
  #     POSTGRES_DB: career_assistant

  #   ports:
  #     - "5432:5432"

  #   volumes:
  #     - pgdata:/var/lib/postgresql/data

  # redis:
  #   image: redis:7-alpine
  #   container_name: career-assistant-redis
  #   restart: always

  #   ports:
  #     - "6379:6379"

  #   volumes:
  #     - redisdata:/data

  chromadb:
    image: chromadb/chroma
    container_name: career-assistant-chroma
    restart: always

    ports:
      - "8000:8000"

    volumes:
      - chromadata:/chroma/chroma

volumes:
  # pgdata:
  # redisdata:
  chromadata: