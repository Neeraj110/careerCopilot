name: Keep Render App Alive
on:
  schedule:
    - cron: '0 */1 * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render app
        run: curl -sSf https://smart-desk-sql.onrender.com/ || echo "Ping failed, but continuing"
